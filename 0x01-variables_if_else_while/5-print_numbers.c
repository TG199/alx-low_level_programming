#include <stdio.h>

/**
 * main - Entry point of program.
 *
 * Description: This program prints all single digit of
 * base 10 starting from 0.
 * Return: 0 (Success).
 */
int main(void)
{
	int i;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	} printf("\n");
	return (0);
}
